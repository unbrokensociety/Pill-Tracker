> app-version: 2.4.8

* CI снова зелёный: opt-in debug-подписи переехал в gradle.properties
* Баннер о debug-подписи в каждом логе; строгий режим без opt-in остался
* Сертификат не менялся — обновления ставятся поверх, стекло и рефакторинг при себе
