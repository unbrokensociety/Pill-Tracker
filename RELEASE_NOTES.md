> app-version: 2.4.13

* Bottom bar glass is back to the classic Telegram-style recipe: a real
  30dp backdrop blur with a light scrim and a whisper-low tint
* No iOS lens, no refraction, no white film — the blur itself is the look
* Android 8-11 keep the soft CPU blur; battery saver falls back to the
  calm translucent bar; cards and everything else are untouched
